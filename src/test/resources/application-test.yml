spring:
  config:
    use-legacy-processing: true
  profiles:
    include:
      - flyway-test
      - mysql-test
      - security-test

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE